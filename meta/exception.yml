---
exceptions:
  - reason: Testing mysql fails, because a dependecy (ansible-role-mysql) is not met.
    variation: amazonlinux:1
  - reason: Testing mysql fails, because a dependecy (ansible-role-mysql) is not met.
    variation: alpine
